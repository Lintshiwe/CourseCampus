
rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Allow file reads for everyone
    // Allow file writes only for authenticated users (admins) in the 'materials' folder
    match /materials/{allPaths=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
